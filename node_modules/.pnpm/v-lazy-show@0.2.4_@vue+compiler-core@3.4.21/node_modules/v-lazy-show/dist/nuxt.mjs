import { transformLazyShow } from './index.mjs';
import '@vue/compiler-core';

function nuxt(_, nuxt) {
  var _a, _b, _c;
  (_a = nuxt.options).vue || (_a.vue = {});
  (_b = nuxt.options.vue).compilerOptions || (_b.compilerOptions = {});
  (_c = nuxt.options.vue.compilerOptions).nodeTransforms || (_c.nodeTransforms = []);
  nuxt.options.vue.compilerOptions.nodeTransforms.unshift(transformLazyShow);
}

export { nuxt as default };
