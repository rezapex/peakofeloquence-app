'use strict';

const compilerCore = require('@vue/compiler-core');

const indexMap = /* @__PURE__ */ new WeakMap();
const PATCH_FLAGS = {
  STABLE_FRAGMENT: "64"
};
const DIRECTIVE_NODES = {
  SHOW: "show"
};
const transformLazyShow = compilerCore.createStructuralDirectiveTransform(
  /^(lazy-show|show)$/,
  (node, dir, context) => {
    if (dir.name === DIRECTIVE_NODES.SHOW && !dir.modifiers.includes("lazy")) {
      return () => {
        node.props.push(dir);
      };
    }
    const directiveName = dir.name === DIRECTIVE_NODES.SHOW ? "v-show.lazy" : "v-lazy-show";
    if (node.tag === "template")
      throw new Error(`${directiveName} can not be used on <template>`);
    const conditionExp = dir.exp;
    node.props.forEach((prop) => {
      if ("exp" in prop && prop.exp && "content" in prop.exp && prop.exp.loc.source)
        prop.exp = compilerCore.createSimpleExpression(prop.exp.loc.source);
    });
    if (conditionExp.loc.source)
      dir.exp = compilerCore.createSimpleExpression(conditionExp.loc.source);
    if (context.ssr || context.inSSR) {
      const ssrTransformIf = context.nodeTransforms[0];
      node.props.push({
        ...dir,
        modifiers: dir.modifiers.filter((modifier) => modifier !== "lazy"),
        name: "if"
      });
      ssrTransformIf(node, context);
      return;
    }
    const { helper } = context;
    const keyIndex = (indexMap.get(context.root) ?? 0) + 1;
    indexMap.set(context.root, keyIndex);
    const key = `_lazyshow${keyIndex}`;
    const wrapNode = compilerCore.createConditionalExpression(
      compilerCore.createCompoundExpression([`_cache.${key}`, " || ", conditionExp]),
      compilerCore.createSequenceExpression([
        compilerCore.createCompoundExpression([`_cache.${key} = true`]),
        compilerCore.createVNodeCall(
          context,
          helper(compilerCore.FRAGMENT),
          void 0,
          [node],
          PATCH_FLAGS.STABLE_FRAGMENT,
          void 0,
          void 0,
          true,
          false,
          false,
          node.loc
        )
      ]),
      compilerCore.createCallExpression(helper(compilerCore.CREATE_COMMENT), [
        '"v-show-if"',
        "true"
      ])
    );
    node.props.push({
      ...dir,
      modifiers: dir.modifiers.filter((modifier) => modifier !== "lazy"),
      name: DIRECTIVE_NODES.SHOW
    });
    context.replaceNode(wrapNode);
    return () => {
      if (!node.codegenNode)
        compilerCore.traverseNode(node, context);
    };
  }
);

exports.transformLazyShow = transformLazyShow;
