import { JSType, Schema, InputValue } from 'untyped';

type ConfigInputsTypes = Exclude<JSType, 'symbol' | 'function' | 'any' | 'bigint'> | 'default' | 'icon' | 'file' | 'media' | 'component' | 'design-token';
type DesignTokensInputsTypes = 'color' | 'size' | 'shadow' | 'size' | 'opacity' | 'font' | 'font-weight' | 'font-size' | 'letter-spacing';
type PickerTypes = 'media-picker' | 'icon-picker';
type InputsTypes = DesignTokensInputsTypes | ConfigInputsTypes;
type PartialSchema = Pick<Schema, 'title' | 'description' | 'default' | 'required'> & {
    [key: string]: any;
};
declare const supportedFields: {
    [key in InputsTypes]: Schema;
};
type StudioFieldData = PartialSchema & {
    type?: keyof typeof supportedFields;
    icon?: string;
    fields?: {
        [key: string]: InputValue;
    };
};
/**
 * Helper to build aNuxt Studio compatible configuration schema.
 * Supports all type of fields provided by Nuxt Studio and all fields supported from Untyped Schema interface.
 */
declare function field(schema: StudioFieldData): InputValue;
declare function group(schema: StudioFieldData): InputValue;

export { type ConfigInputsTypes, type DesignTokensInputsTypes, type InputsTypes, type PartialSchema, type PickerTypes, type StudioFieldData, field, group };
