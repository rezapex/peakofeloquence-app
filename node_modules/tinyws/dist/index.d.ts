/// <reference types="node" />
import ws from 'ws';
import http from 'http';
export interface TinyWSRequest extends http.IncomingMessage {
    ws: () => Promise<ws>;
}
/**
 * tinyws - adds `req.ws` method that resolves when websocket request appears
 * @param wsOptions
 */
export declare const tinyws: (wsOptions?: any, wss?: any) => (req: TinyWSRequest, _: unknown, next: () => void | Promise<void>) => Promise<void>;
